{% extends 'base.html.twig' %}

{% block title %}Hello GuestController!{% endblock %}

{% block body %}
    <div class="row wrapper text-center">
        <div class="col-3"></div>
        <div class="col-6">
            <div class="rounded-1 mt-5 pt-1 bg-secondary">
                <div class="d-flex justify-content-center">
                    <div class="w-md-50 w-lg-25 fs-2 bg-secondary-subtle d-flex">
                        <div>
                            <img class="img-fluid" src="{{ asset('img/logos/cogwheel.png') }}" alt="cogwheel image">
                        </div>
                        Settings
                        <div>
                            <img class="img-fluid" src="{{ asset('img/logos/cogwheel.png') }}" alt="cogwheel image">
                        </div>
                    </div>
                </div>
                
            </div>
            <div class="mt-5">
                <a class="btn py-4 fs-2 bg-lightgreen border border-5 border-black rounded-5 fm-courier fw-bolder"
                   href="{{ path("app_home") }}">
                    Back to home
                </a>
            </div>
        </div>
        <div class="col-3"></div>
    </div>
{% endblock %}

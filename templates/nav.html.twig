{% block nav %}
    <nav class="navbar navbar-expand-xl bg-darkgreen mb-5">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_home') }}"><img class="w-75 img-fluid"
                                                                       src="{{ asset('img/logos/Dabloon.jpg') }}"
                                                                       alt="dabloon image to home"></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="w-100 row gx-6 gy-3 gy-xl-0 navbar-nav text-center px-3 fs-5">
                    {% if app.user is null %}
                        <li class="nav-item col-12 col-xl-2">
                            <div class="bg-lightgreen rounded-1">
                                <a class="nav-link active" aria-current="page" href="{{ path('app_home') }}">Home</a>
                            </div>
                        </li>
                        <li class="nav-item col-12 col-xl-2">
                            <div class="bg-lightgreen rounded-1">
                                <a class="nav-link" href="{{ path('app_guest_settings') }}">Settings</a>
                            </div>
                        </li>
                        <li class="nav-item col-12 col-xl-2">
                            <div class="bg-lightgreen rounded-1">
                                <a class="nav-link" href="{{ path('app_about') }}">About</a>
                            </div>
                        </li>
                        <li class="nav-item col-12 col-xl-2">
                            <div class="bg-lightgreen rounded-1">
                                {#                        <a class="nav-link disabled" href="#"></a> #}
                            </div>
                        </li>
                        <li class="nav-item col-12 col-xl-2">
                            <div class="bg-lightgreen rounded-1">
                                <a class="nav-link" aria-disabled="true" href="{{ path('app_register') }}">Register</a>
                            </div>
                        </li>
                        <li class="nav-item col-12 col-xl-2">
                            <div class="bg-lightgreen rounded-1">
                                <a class="nav-link" aria-disabled="true" href="{{ path('app_login') }}">Login</a>
                            </div>
                        </li>
                    {% else %}
                        {# The user navbars #}
                        {% if 'ROLE_ADMIN' in app.user.roles %}
                            {# Admin #}
                        {% elseif 'ROLE_MODERATOR' in app.user.roles %}
                            {# Moderator #}
                        {% elseif 'ROLE_USER' in app.user.roles %}
                            {# Member #}
                            <li class="nav-item col-12 col-xl-2">
                                <div class="bg-lightgreen rounded-1">
                                    <a class="nav-link active" aria-current="page"
                                       href="{{ path('app_home') }}">Home</a>
                                </div>
                            </li>
                            <li class="nav-item col-12 col-xl-2">
                                <div class="bg-lightgreen rounded-1">
                                    <a class="nav-link" href="{{ path('app_guest_settings') }}">Settings</a>
                                </div>
                            </li>
                            <li class="nav-item col-12 col-xl-2">
                                <div class="bg-lightgreen rounded-1">
                                    <a class="nav-link" href="{{ path('app_about') }}">About</a>
                                </div>
                            </li>
                            <li class="nav-item col-12 col-xl-2">
                                <div class="bg-lightgreen rounded-1">
                                    {#                            <a class="nav-link disabled" href="#"></a> #}
                                </div>
                            </li>
                            <li class="nav-item col-12 col-xl-2">
                                <div class="bg-lightgreen rounded-1">
                                    <a class="nav-link" aria-disabled="true" href="{{ path('app_profile') }}">Profile</a>
                                </div>
                            </li>
                            <li class="nav-item col-12 col-xl-2">
                                <div class="bg-lightgreen rounded-1">
                                    <a class="nav-link" aria-disabled="true" href="{{ path('app_logout') }}">Logout</a>
                                </div>
                            </li>
                        {% endif %}
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}
